apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: checkout-api

resources:
  - ../../base

# Environment-specific values live here, never in base.
replicas:
  - name: checkout-api
    count: 1
